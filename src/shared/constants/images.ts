export const images = {
  /** Transparent heart — same asset for light and dark (charte: the logo
   *  keeps its colors on dark backgrounds). */
  logoDark: require('@/assets/images/logo-dark.png'),
  logoLight: require('@/assets/images/logo-light.png'),
  /** Official circular app icon (heart on brume pastille). */
  logoRound: require('@/assets/images/logo-round.png'),
  /** White silhouette — monochrome contexts on deep backgrounds. */
  logoSilhouette: require('@/assets/images/logo-silhouette.png'),
};
